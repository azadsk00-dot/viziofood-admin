import { useCallback } from 'react'; import { useResource } from '../admin/useResource'; import { getOrders, updateOrderStatus } from '../admin/supabase'; import type { OrderStatus } from '../admin/types'; import { useOrdersRealtime } from '../hooks/useOrdersRealtime'; import { useToast } from '../components/Toast';
const next:Partial<Record<OrderStatus,OrderStatus>>={New:'Accepted',Accepted:'Preparing',Preparing:'Ready',Ready:'Completed'};
export default function Kitchen(){const orders=useResource(getOrders);const toast=useToast();const refresh=useCallback(()=>void orders.reload(),[orders.reload]);useOrdersRealtime(refresh);const advance=async(id:string,status:OrderStatus)=>{try{await updateOrderStatus(id,status);toast.show(`Order ${id} updated`);await orders.reload()}catch{toast.show('Could not update order','error')}};return <main className="kitchen"><header><p className="eyebrow">Kitchen display</p><h1>Live orders</h1></header>{orders.loading?<p>Loading orders…</p>:orders.error?<p className="admin-message error">{orders.error}</p>:!orders.data?.length?<p className="admin-message">No live orders.</p>:<div className="kitchen-grid">{orders.data.filter(order=>order.status!=='Completed'&&order.status!=='Cancelled').map(order=><article className="kitchen-card" key={order.id}><div><span className="status">{order.status}</span><h2>{order.id}</h2></div><p>{order.customer}</p><b>{order.items} item{order.items===1?'':'s'} · ${order.total.toFixed(2)}</b>{order.notes&&<p className="kitchen-note">Note: {order.notes}</p>}{next[order.status]&&<button className="admin-primary" onClick={()=>advance(order.id,next[order.status]!)}>{next[order.status]}</button>}</article>)}</div>}</main>}

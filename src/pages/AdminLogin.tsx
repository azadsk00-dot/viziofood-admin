import { useState, type FormEvent } from 'react'; import { Link, useNavigate } from 'react-router-dom'; import { useAuth } from '../context/AuthProvider';
export default function AdminLogin(){const {signIn,resetPassword,loading}=useAuth();const navigate=useNavigate();const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [remember,setRemember]=useState(true);const [message,setMessage]=useState<string>();const [error,setError]=useState<string>();const [busy,setBusy]=useState(false);const submit=async(event:FormEvent)=>{event.preventDefault();setBusy(true);setError(undefined);try{const profile=await signIn(email,password);if(!remember)sessionStorage.setItem('vizio-session-only','true');if(profile.role!=='admin'){setError('Access Denied. This account does not have administrator access.');navigate('/',{replace:true});return}navigate('/admin',{replace:true})}catch(reason){setError(reason instanceof Error?reason.message:'Unable to sign in.')}finally{setBusy(false)}};return <main className="admin-login"><form className="login-card" onSubmit={submit} noValidate><Link className="brand" to="/">VIZIO <i>FOOD</i></Link><p className="eyebrow">Admin access</p><h1>Welcome back.</h1><label>Email<input value={email} onChange={e=>setEmail(e.target.value)} type="email" autoComplete="email" placeholder="you@viziofood.com" required/></label><label>Password<input value={password} onChange={e=>setPassword(e.target.value)} type="password" autoComplete="current-password" required/></label><label className="remember"><input checked={remember} onChange={e=>setRemember(e.target.checked)} type="checkbox"/>Remember me</label><button className="textlink forgot" type="button" onClick={async()=>{if(!email){setError('Enter your email address first.');return}try{await resetPassword(email);setMessage('Password reset email sent.')}catch(reason){setError(reason instanceof Error?reason.message:'Unable to send reset email.')}}}>Forgot password?</button>{error&&<p className="login-error" role="alert">{error}</p>}{message&&<p className="login-success" role="status">{message}</p>}<button className="admin-primary" disabled={busy||loading} type="submit">{busy||loading?'Signing in…':'Sign in'}</button><small>Secure access for Vizio Food staff.</small></form></main>}

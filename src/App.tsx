import { lazy, Suspense } from 'react'; import { Navigate, Route, Routes } from 'react-router-dom'; import { Navbar, Footer } from './components/Layout'; import { ErrorBoundary } from './components/ErrorBoundary'; import { AdminRoute, StaffRoute } from './components/ProtectedRoute'; import { Categories, Customers, Reports, SettingsPage } from './admin/pages'; import ProductManagement from './admin/ProductManagement'; import { EnhancedDashboard, EnhancedOrders } from './admin/AdminEnhanced'; import { CheckoutCancel, CheckoutSuccess } from './pages/CheckoutResult'; import AdminLogin from './pages/AdminLogin';
const Home=lazy(()=>import('./pages/Home'));const Menu=lazy(()=>import('./pages/Menu'));const About=lazy(()=>import('./pages/About'));const Checkout=lazy(()=>import('./pages/Checkout'));const Account=lazy(()=>import('./pages/Account'));const Kitchen=lazy(()=>import('./pages/Kitchen'));const AdminLayout=lazy(()=>import('./admin/AdminLayout'));
function Public(){return <><a className="skip-link" href="#main-content">Skip to content</a><Navbar/><main id="main-content"><Routes><Route path="/" element={<Home/>}/><Route path="/menu" element={<Menu/>}/><Route path="/about" element={<About/>}/><Route path="/account" element={<Account/>}/><Route path="/checkout" element={<Checkout/>}/><Route path="/checkout/success" element={<CheckoutSuccess/>}/><Route path="/checkout/cancel" element={<CheckoutCancel/>}/><Route path="*" element={<Navigate to="/" replace/>}/></Routes></main><Footer/></>}
export default function App(){return <ErrorBoundary><Suspense fallback={<div className="admin-message">Loading…</div>}><Routes><Route path="/kitchen" element={<StaffRoute><Kitchen/></StaffRoute>}/><Route path="/admin/login" element={<AdminLogin/>}/><Route path="/admin" element={<AdminRoute><AdminLayout/></AdminRoute>}><Route index element={<EnhancedDashboard/>}/><Route path="orders" element={<EnhancedOrders/>}/><Route path="products" element={<ProductManagement/>}/><Route path="categories" element={<Categories/>}/><Route path="customers" element={<Customers/>}/><Route path="reports" element={<Reports/>}/><Route path="settings" element={<SettingsPage/>}/></Route><Route path="*" element={<Public/>}/></Routes></Suspense></ErrorBoundary>}

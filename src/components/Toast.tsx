import { createContext, useCallback, useContext, useState, type ReactNode } from 'react';
type Toast={id:number;message:string;type:'success'|'error';action?:{label:string;onClick:()=>void}};type Options={type?:Toast['type'];action?:Toast['action']};const ToastContext=createContext<{show:(message:string,options?:Toast['type']|Options)=>void}>({show:()=>undefined});
export function ToastProvider({children}:{children:ReactNode}){const [toasts,setToasts]=useState<Toast[]>([]);const show=useCallback((message:string,options:Toast['type']|Options='success')=>{const config=typeof options==='string'?{type:options}:options;const id=Date.now();setToasts(items=>[...items,{id,message,type:config.type??'success',action:config.action}]);window.setTimeout(()=>setToasts(items=>items.filter(t=>t.id!==id)),5000)},[]);return <ToastContext.Provider value={{show}}>{children}<div className="toasts" aria-live="polite">{toasts.map(t=><div className={`toast ${t.type}`} key={t.id}>{t.message}{t.action&&<button onClick={()=>{t.action?.onClick();setToasts(items=>items.filter(item=>item.id!==t.id))}}>{t.action.label}</button>}</div>)}</div></ToastContext.Provider>};export const useToast=()=>useContext(ToastContext);

import { Link, useSearchParams } from 'react-router-dom'; import { CheckCircle2, XCircle } from 'lucide-react';
export function CheckoutSuccess(){const [params]=useSearchParams();return <main className="empty"><CheckCircle2 size={50}/><p className="eyebrow">Payment successful</p><h1>Grazie.</h1><p>Your order has been confirmed{params.get('session_id')?' and is now with the kitchen.':' .'}</p><Link className="button" to="/">Return home</Link></main>}; export function CheckoutCancel(){return <main className="empty"><XCircle size={50}/><p className="eyebrow">Payment cancelled</p><h1>Your cart is saved.</h1><p>No payment was taken.</p><Link className="button" to="/checkout">Return to checkout</Link></main>}
